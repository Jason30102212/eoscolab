const initialState = [
  {id: 1, skillName: "JavaScript"},
  {id: 2, skillName: "Eos Blockchain Development"}
];

const skillSet = (state = initialState, action) => {
  return state;
}

export default skillSet;
